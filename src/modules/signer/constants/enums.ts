export enum WalletType {
  SECRETKEY = 'privateKey',
  ALBEDO = 'albedo',
  FREIGHTER = 'freighter',
  RABET = 'rabet',
  WALLET_CONNECT = 'walletConnect',
  XBULL = 'xbull',
}

export enum MessageEventType {
  CONNECT = 'onConnect',
  CANCEL = 'onCancel',
  SIGN = 'onSign',
  READY = 'onReady',
}
